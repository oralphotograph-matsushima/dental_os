# Wireless Connect 変更点

院内 PC へのソース移植は `Clinic_Distribution_Assets/2. WirelessConnect/` の zip を使う。  
このファイルは **1.5.0 → 1.5.1** で入れた運用上の差分。

## 1.5.1（2026-08-31）

Windows 移植 zip: `WirelessConnect_v1.5.1_ソース移植_20260831.zip`

### 患者フォルダ

- 患者の特定は **番号だけ**。OCR の氏名がずれても同じ ID なら同じフォルダ。
- フォルダ名は `番号_カタカナ` を優先。bare の `番号` だけが残っていたら既存の `番号_名前` へ寄せる。
- **空フォルダは作らない。** 受信開始やアポ選択ではメモリ上の対象患者だけ切替える。実フォルダは **最初の写真** または **最初のカルテ保存** のとき作る。
- 起動時に、中身のない患者フォルダ（`layout.json` だけのものを含む）を掃除。

### カルテと写真

- カルテ（`カルテ_*.md`）と口腔内写真は **同じ患者フォルダ** に置く。
- Obsidian 向けに、カルテ末尾へ `![[ファイル名]]` の wikilink を付ける。画像バイトは埋め込まない。合成 PNG（latest）を先に出す。
- 履歴・患者一覧は患者フォルダ内のカルテを読む（旧 `カルテ/` もフォールバック）。

### Wireless Connect（配置確認）

- 本日の予約を読み込んだあと、患者をタップすると既存写真を PC / iPad で確認できる。
- テンプレートは **5 / 7 / 9**。既定は 7。
- 確認コピーは「この配置でよいですか？」。Vision は枠と `鏡？` まで。自動では反転しない。
- 枠を 2 回タップ（または同じ枠）で編集シート。A → B で入れ替え。
- 確定後、透過 PNG を患者フォルダへ保存する。
  - `oral_{5|7|9}view_{番号}_{YYYYMMDD}_{HHmm}.png`
  - `oral_{n}view_latest.png`（上書き。スライドタブが拾う）
- 画面プレビューは黒背景。書き出し PNG の背景は透明。

### スライドタブ

- 最上段は **口腔内 9 枚**（パノラマを最上にしない）。
- PPTX / PDF は出さない。PNG のみ。
- 9 枚の並び（左→右、上→下）:
  1. 右側側方（半開口）
  2. 上顎
  3. 左側側方（半開口）
  4. 右側
  5. 正面
  6. 左側
  7. カップリング
  8. 下顎
  9. 半開口
- 7 枚法は同じ 3×3 で、左右側方（半開口）の 2 枠を空ける。
- ラベルは折り返す（例: `右側側方` / `（半開口）`）。

### ブランド

- アプリアイコン・LP のロゴは **オレンジ基調の Wireless Connect**。
- OralNote 時代の LP 写真（黒背景 × 人物・バナー）とエメラルドグリーンのロゴ画像は削除した。

### 移植時に入れないもの

- `.env.local`（動かす PC 側の既存ファイルを使う）
- `node_modules` / `.next` / `dist` / `.git`

Windows 側:

```bat
npm install
npm run build:win
```

インストーラは `dist` に出る。
