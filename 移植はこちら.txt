# 移植パッケージの場所

このフォルダ直下に zip は置かない。

コピーするもの:
`../Clinic_Distribution_Assets/2. WirelessConnect/WirelessConnect_v1.5.1_ソース移植_20260831.zip`

変更点はリポジトリの `CHANGELOG.md`（zip 内にも入る）。

古い zip は同フォルダの `_past_使わない/` に隔離済み。
